from pathlib import Path


def run_tests(path: Path) -> None:
    print(f"Running tests found in {str(path)}...")


def review_snaps(path: Path) -> None:
    print(f"Reviewing snaps found in {str(path)}...")
